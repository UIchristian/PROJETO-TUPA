"            <dt className=\"text-muted-foreground\">{t(\"profile.size\")}</dt>\n            <dd className=\"font-semibold text-foreground\">{profileArea} ha</dd>\n          </dl>\n\n          {/* Terrains Section */}\n          <div className=\"mt-4 border-t border-border/60 pt-3.5 flex flex-col gap-3\">\n            <span className=\"text-[13px] font-bold text-foreground/80 flex justify-between items-center\">\n              <span>{language === \"es\" ? \"Terrenos Registrados\" : language === \"en\" ? \"Registered Terrains\" : \"Terrenos Cadastrados\"}</span>\n              <span className=\"text-[11.5px] font-bold text-primary bg-primary/10 px-2.5 py-0.5 rounded-full shrink-0\">\n                Total: {parsedTerrenos.reduce((acc, curr) => acc + (curr.hectares || 0), 0).toFixed(2)} ha\n              </span>\n            </span>\n            <div className=\"flex flex-col gap-2.5\">\n              {parsedTerrenos.map((terreno) => (\n                <div key={terreno.id} className=\"p-3 bg-soft/50 border border-border/50 rounded-xl flex flex-col gap-2 shadow-soft\">\n                  <div className=\"flex justify-between items-start gap-2\">\n                    <div className=\"min-w-0 flex-1\">\n                      {editingNameId === terreno.id ? (\n                        <div className=\"flex items-center gap-1\">\n                          <input\n                            value={tempNameVal}\n                            onChange={(e) => setTempNameVal(e.target.value)}\n                            className=\"h-8 px-2 bg-white border border-border rounded text-[12px] outline-none font-semibold text-foreground w-full\"\n                          />\n                          <button\n                            type=\"button\"\n                            onClick={async () => {\n                              const updated = parsedTerrenos.map((t) => t.id === terreno.id ? { ...t, name: tempNameVal } : t);\n                              await updateProfileFields({\n                                terrenos
<truncated 8895 bytes>