# Tupã Frontend — Environment Variables
# Copy to .env and adjust as needed

# Backend API URL
VITE_API_URL=http://localhost:8000

# Use mock data instead of real API (true | false)
VITE_USE_MOCK=false
